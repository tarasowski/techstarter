1. Spin up EC2 instance
2. Run a container from your custom-image on EC2
	* first install docker
	* then launch docker daemon
	* if docker launched then run a container
3. Make your EC2 accessible on the web
